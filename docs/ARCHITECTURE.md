# Farsi_RTL — توضیح کامل پروژه

سندی برای کسی که پروژه را ندیده و می‌خواهد بفهمد چه می‌کند، چطور کار می‌کند،
کجا شکننده است، و قدم بعدی چه گزینه‌هایی دارد.

نسخهٔ فعلی: **v1.36.0** · افزونهٔ Chrome/Firefox، MV3، بدون وابستگی خارجی.

---

## ۱. مسئله

مدل‌های هوش مصنوعی برای کاربر فارسی‌زبان جوابی می‌نویسند که فارسی است ولی پر از
اصطلاح انگلیسی و بلوک کد. مرورگر نمی‌داند هر پاراگراف به چه زبانی نوشته شده، و
سایت‌ها هم جهت را به‌درستی ست نمی‌کنند. نتیجه:

| علامت | علت |
|---|---|
| جواب فارسی چپ‌چین می‌شود | جهت پاراگراف ست نشده |
| نقطه و «؟» سر جای غلط می‌افتد | الگوریتم bidi با جهت پایهٔ غلط |
| ستون‌های جدول فارسی برعکس خوانده می‌شود | `direction` روی `<table>` ست نشده |
| تایپ فارسی در کادر پرامپت به‌هم می‌ریزد | کادر ورودی چپ‌چین است |

---

## ۲. معماری

| فایل | نقش | DOM؟ |
|---|---|---|
| `engine.js` | تصمیم زبان — توابع خالص | ندارد |
| `match.js` | تطبیق الگوی سایت | ندارد |
| `content.js` | مشاهدهٔ صفحه، علامت‌گذاری، ایزوله‌کردن، تشخیص، خاموش/روشن | دارد |
| `popup.js` / `popup.html` | تنظیمات | دارد |
| `background.js` | فقط رلهٔ میان‌بر صفحه‌کلید | ندارد |
| `tools/accuracy.js` | اجراگر دقت روی مجموعهٔ تست | Node |
| `tools/match.test.js` | تست تطبیق الگو | Node |

`engine.js` و `match.js` عمداً بدون DOM نوشته شده‌اند تا **همان کدی که در
مرورگر اجرا می‌شود** مستقیماً در Node تست شود، نه یک کپی.

---

## ۳. موتور تصمیم

سؤال این نیست که کدام زبان بیشترِ پاراگراف را گرفته، بلکه این است که پاراگراف
به چه زبانی *نوشته* شده.

ابتدا کد داخل بک‌تیک و URLها کنار گذاشته می‌شوند: یک شناسه داخل `<code>` شیء
خارجی‌ای است که در جمله افتاده، نه زبانی که جمله با آن نوشته شده. بعد آنچه
می‌ماند («نثر») با این قواعد سنجیده می‌شود — به ترتیب، اولین قاعده‌ای که صدق
کند جواب را می‌دهد:

| # | شرط | نتیجه | توضیح |
|---|---|---|---|
| ۱ | نثر فارسی ندارد | دست نزن | پاراگراف انگلیسی |
| ۲ | نثر لاتین ندارد | RTL | فارسی خالص |
| ۳ | اولین حرف قویِ نثر فارسی است | RTL | همان first-strong پشت `dir="auto"` |
| ۴ | کلمات فارسی ≥ کلمات لاتین | RTL | **کلمه** می‌شمارد نه حرف |
| ۵ | فارسی بین دو ران لاتین آمده | RTL | «و» آنجا حرف ربط است، نه شیء |
| ۶ | لاتین ≥ ۴ کلمه و فارسی فقط در دم | LTR | جملهٔ انگلیسی با شیء فارسی |
| ۷ | وگرنه | RTL | لاتینِ کوتاه = اصطلاح، نه جمله |

### چرا «کلمه» نه «حرف»

شمردن حروف، جواب را به طول شناسه وابسته می‌کند:

| متن | با شمارش حرف | با قواعد فعلی | درست |
|---|---|---|---|
| متد `getClass()` | RTL | RTL (قاعدهٔ ۳) | RTL |
| متد `isAnnotationPresent()` | **LTR** ❌ | RTL (قاعدهٔ ۳) | RTL |
| JNI چیست | RTL | RTL (قاعدهٔ ۴) | RTL |
| native method چیست | **LTR** ❌ | RTL (قاعدهٔ ۷) | RTL |
| Compile Time و Runtime | LTR | RTL (قاعدهٔ ۵) | RTL |
| The Persian word for runtime is زمان اجرا | — | LTR (قاعدهٔ ۶) | LTR |

تصمیم‌گیری **همگام و قطعی** است: همان پاراگراف همیشه همان جواب را می‌گیرد،
مستقل از ترتیب پردازش صفحه.

### چه چیزهایی عمداً حذف شدند

| سیگنال | چرا حذف شد |
|---|---|
| CLD3 (`chrome.i18n.detectLanguage`) | روی رشته‌های کوتاه و مخلوط غیرقابل‌اتکا؛ async بود و فلیکر می‌ساخت |
| نسبت کل صفحه | نتیجه را به ترتیب پردازش وابسته می‌کرد — یک تیتر در دو بار لود دو جواب می‌گرفت |

---

## ۴. چرخهٔ حیات در صفحه

```
MutationObserver → صف → بودجهٔ زمانی → تصمیم جهت → (بعد از آرام‌شدن) ایزوله‌کردن
```

قانون مرکزی پروژه این است:

> جهت‌دهی (یک اتریبیوت + دو استایل اینلاین) هر لحظه امن است.
> تغییر **ساختار** DOM فقط وقتی امن است که چیزی در حال نوشته‌شدن نباشد.

دلیلش: ChatGPT و Claude جواب را با React رندر می‌کنند و React رفرنس مستقیم به
همان متن‌نودها نگه می‌دارد. جایگزین‌کردن یک متن‌نود وسط استریم، رندرِ مارک‌داون
را می‌کُشد و کل جواب به‌صورت خام در یک بلوک می‌ریزد.

| محافظ | کار |
|---|---|
| تایمر سکوت هر المان | ۴۵۰ms بدون تغییر متن |
| سیگنال سایت | مارکرهای «در حال تولید» (دکمهٔ stop و…) |
| سنجهٔ نرخ موتیشن | پشتیبانِ مستقل از سایت، اگر مارکرها عوض شوند |
| انجماد | پاراگرافی که جهت گرفته و بلند است، تا پایان استریم دوباره خوانده نمی‌شود |
| پرش از کد | `<pre>` و `<code>` هرگز بازساخته نمی‌شوند |

---

## ۵. قابلیت‌ها

| قابلیت | جزئیات | ذخیره در |
|---|---|---|
| سه حالت راست‌چین | هوشمند / همیشه / هیچ‌وقت | `sync.rtlMode` |
| فونت Vazirmatn | سه وزن، باندل‌شده | `sync.fontEnabled` |
| فاصلهٔ سطرها | فقط پاراگراف RTL؛ کد دست‌نخورده | `sync.lineSpacing` |
| جهت کادر تایپ | `dir="auto"` روی هر پاراگراف ورودی | — |
| جدول‌ها | `direction` روی `<table>` = ترتیب ستون | — |
| اصلاح دستی | دکمهٔ ⇄ یا `Alt+Shift+F` | `local.overrides` (سقف ۵۰۰) |
| سوییچ هر سایت | با teardown کامل | `sync.siteOff` |
| حالت تشخیص | مرز رنگی + شمارهٔ قاعده + خروجی JSON | `sync.debugMode` |

«خاموش» یعنی واقعاً خاموش: مارک‌ها، استایل‌های اینلاین، `dir`ها، رَپِرهای
`<bdi>` (با `normalize()`)، استایل‌شیت و observer همه برداشته می‌شوند. دلیلش
این است که `<bdi>` در استایل‌شیت خود مرورگر `unicode-bidi: isolate` دارد، پس
جاگذاشتنش یعنی صفحه همچنان فرق دارد.

### سایت‌ها

chatgpt.com · chat.openai.com · claude.ai · chat.deepseek.com ·
gemini.google.com · grok.com · perplexity.ai · uxpilot.ai

بیرون از این فهرست کانتنت‌اسکریپت اصلاً لود نمی‌شود. `host_permissions` حذف شده
چون برای تزریق و بارگذاری فونت لازم نبود (تجربی تأیید شد) و پرامپت نصب را
سنگین می‌کرد.

---

## ۶. پرفورمنس

اندازه‌گیری با Chrome DevTools Protocol روی صفحه‌ای با ۱۵۰ پاراگراف و ۲۶۰
توکن استریم:

| سنجه | بدون افزونه | با افزونه |
|---|---|---|
| p50 فریم | 16.70ms | 16.70ms |
| p95 فریم | 16.90ms | 16.80ms |
| long task | ۰ | ۰ |
| CPU در پنجرهٔ استریم | 1.8ms | ~7ms |

روی سنجش ۳۰۰ پاراگرافی بدون استریم: **۱۹ms در برابر ۲۰ms نسخهٔ ۱.۲۳.۳** —
یعنی با وجود همهٔ کارهای اضافه، از نسخهٔ قدیمی سبک‌تر است.

بزرگ‌ترین صرفه‌جویی‌ها: انجماد پاراگرافِ تصمیم‌گرفته‌شده (خواندن `textContent`
در هر فریم گران‌ترین کار بود)، حذف کامل رفت‌وبرگشت صف برای المان منجمد، و
امضای تغییر متن به‌جای هش کل پاراگراف.

---

## ۷. تست

| مجموعه | تعداد | چه چیزی را می‌سنجد |
|---|---|---|
| `tools/accuracy.js` | ۸۲ مورد | دقت تصمیم، تفکیک به‌ازای هر قاعده |
| `tools/match.test.js` | ۲۰ URL | تطبیق الگوی سایت |
| تست‌های مرورگر (Playwright) | ۱۰ مجموعه، ۹۲ ادعا | استریم، صف، حافظه، مودها، جدول، کادر تایپ، خاموش/روشن، میان‌بر، پاپ‌آپ |

۲۰ مورد از مجموعهٔ دقت از گفتگوهای واقعی برداشت شده. حالت تشخیص این کار را
می‌کند: تصمیم‌های کل صفحه را با شمارهٔ قاعده به‌صورت JSON کپی می‌کند.

**نکتهٔ روش‌شناختی:** مقدار `expect` در خروجیِ برداشت، جوابِ خودِ موتور است.
اگر بدون بازبینی به مجموعه اضافه شود، دقت به‌طور خودکار ۱۰۰٪ می‌شود و هیچ
معنایی ندارد. موردهای برداشت‌شده فقط بعد از بازبینی دستی اضافه می‌شوند.

---

## ۸. محدودیت‌ها و ریسک‌های شناخته‌شده

| مورد | شرح |
|---|---|
| تنظیم‌شده برای نثر گفتگو | قواعد روی جملهٔ فارسی با اصطلاح انگلیسی سنجیده شده‌اند، نه منوی ناوبری، فرم، یا داشبورد |
| مجموعهٔ تست یک‌سبک است | هر سه گفتگوی برداشت‌شده یک موضوع و یک سبک نوشتاری دارند |
| مارکرهای استریم سایت‌محورند | اگر سایتی عوضشان کند، فقط سنجهٔ نرخ موتیشن می‌ماند |
| چهار سایت جدید تست میدانی نشده‌اند | DeepSeek/Gemini/Grok/Perplexity فقط از روی manifest اضافه شده‌اند |
| آورراید با هش ۲۰۰ کاراکتر اول | دو پاراگراف با شروع یکسان یک کلید می‌گیرند |
| بدون i18n کامل | رابط فقط فارسی است؛ فقط توضیح افزونه دوزبانه شد |
| فایرفاکس تست میدانی نشده | لایهٔ سازگاری نوشته شده ولی روی فایرفاکس واقعی اجرا نشده |

---

## ۹. گزینه‌های مرحلهٔ بعد

| گزینه | ارزش | هزینه | ریسک |
|---|---|---|---|
| بزرگ‌کردن مجموعهٔ تست با سبک‌های متنوع‌تر | زیاد — تنها راه سنجش قواعد | کم | ندارد |
| تست میدانی چهار سایت جدید + فایرفاکس | زیاد | کم | ندارد |
| i18n کامل رابط (fa/en/ar) | متوسط — موتور عربی/عبری/اردو را هم می‌فهمد | متوسط | رابط کاربر فعلی ممکن است انگلیسی شود |
| انتشار در Chrome Web Store | زیاد برای دیده‌شدن | متوسط (بررسی، آیکون‌ها، اسکرین‌شات) | ندارد |
| سیگنال «زبان کل گفتگو» برای موارد مبهم | متوسط | متوسط | باید قطعی بماند، نه انباشتی |
| فعال‌سازی روی دامنهٔ دلخواه (`optional_host_permissions`) | متوسط | متوسط | قواعد بیرون از چت‌بات سنجیده نشده‌اند |
| کنترل اندازهٔ فونت | کم–متوسط | کم | ندارد |
| پاک‌کردن آورراید‌ها از پاپ‌آپ | کم | کم | ندارد |

---

## ۱۰. سؤال‌هایی که ارزش پرسیدن دارند

1. با توجه به اینکه مجموعهٔ تست فعلی از یک سبک نوشتاری آمده، چه سبک‌هایی از
   متن فارسی احتمالاً نقطه‌کورِ این هفت قاعده‌اند؟
2. قاعدهٔ ۶ یک آستانهٔ عددی دارد (۴ کلمهٔ لاتین). آیا فرمول‌بندی بهتری بدون
   آستانه وجود دارد که همان کار را بکند؟
3. برای انتشار در Web Store چه چیزهایی کم است؟
4. آیا معماری فعلی (تصمیم همگام + تعویق کار ساختاری) برای سایتی که DOM را
   جور دیگری مدیریت می‌کند هم دوام می‌آورد؟
